// Compatibility entry point for consumers of the former host-local picker.
export {
  readRecentTargets,
  recordRecentTarget,
  removeRecentTarget,
  recentStorageKey,
} from "@agentre-hub/agentre-ui";
